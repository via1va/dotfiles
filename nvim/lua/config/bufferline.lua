-- В вашем конфиге LazyVim
require("bufferline").setup({
  options = {
    separator_style = "none", -- Убирает линию между буферами
    show_buffer_close_icons = false, -- Опционально, скрывает иконки закрытия
    show_close_icon = false, -- Опционально, скрывает иконку закрытия
  },
})
